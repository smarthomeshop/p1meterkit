# P1MeterKit V1 WiFi Configuration (Legacy URL for backwards compatibility)
# New users should use: github://smarthomeshop/p1meterkit/p1meterkit-v1/p1meterkit.yaml@main
#
# This file redirects existing users to the new v1 structure via OTA update.
# After OTA, users will automatically use the new modular YAML structure.

packages:
  base: !include p1meterkit-v1/base.yaml
  wifi: !include p1meterkit-v1/wifi.yaml

dashboard_import:
  package_import_url: github://smarthomeshop/p1meterkit/p1meterkit-v1/p1meterkit.yaml@main
